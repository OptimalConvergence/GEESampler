# Contributing

Create a focused branch, add tests for behavior changes, and run `pytest`,
`ruff check src tests examples`, and `python -m build` before opening a pull
request. Do not commit Earth Engine service-account keys, downloaded imagery, or
licensed source datasets.
